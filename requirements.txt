# Core ML stack
# tensorflow-probability[tf] installs tf-keras (Keras 2) alongside TF, which is
# required because TFP is not compatible with Keras 3 (used in TF 2.16+).
#
# GPU install (requires NVIDIA driver 520+ for CUDA 12):
#   pip install -r requirements.txt
# CPU-only install (swap the line below):
#   tensorflow-cpu==2.21.0
tensorflow[and-cuda]==2.21.0
tensorflow-probability[tf]==0.25.0

# Data / visualization
numpy
matplotlib
Pillow
scipy

# Interactive web UI (interactive_viz.py)
dash
